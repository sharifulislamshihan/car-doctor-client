import { useLoaderData } from "react-router-dom";

const CheckOut = () => {
    const service = useLoaderData
    return (
        <div>
            
        </div>
    );
};

export default CheckOut;